# Supabase — get these from https://app.supabase.com → Project Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Python parser API (Render). Must accept CORS from your app origin and use the same SUPABASE_JWT_SECRET as below.
VITE_PARSER_API_URL=https://your-parser.onrender.com
